/**
 * @deprecated Use o composable `useAuthClient()` ao invés deste import direto.
 * Este arquivo existe apenas para compatibilidade retroativa.
 */
export { useAuthClient as getAuthClient } from '~/composables/useAuthClient';
